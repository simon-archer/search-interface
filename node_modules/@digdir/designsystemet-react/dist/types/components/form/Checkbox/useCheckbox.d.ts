import type { InputHTMLAttributes } from 'react';
import type { FormField } from '../useFormField';
import type { CheckboxProps } from './Checkbox';
type UseCheckbox = (props: CheckboxProps) => FormField & {
    inputProps?: Pick<InputHTMLAttributes<HTMLInputElement>, 'readOnly' | 'type' | 'name' | 'required' | 'defaultChecked' | 'checked' | 'onClick' | 'onChange'>;
};
/** Handles props for `Checkbox` in context with `Checkbox.Group` (and `Fieldset`) */
export declare const useCheckbox: UseCheckbox;
export {};
//# sourceMappingURL=useCheckbox.d.ts.map