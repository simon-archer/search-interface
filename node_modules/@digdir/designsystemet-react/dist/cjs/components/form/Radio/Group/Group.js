'use client';
'use strict';

var jsxRuntime = require('react/jsx-runtime');
var React = require('react');
var lite = require('../../../../node_modules/clsx/dist/lite.js');
var Fieldset = require('../../Fieldset/Fieldset.js');

const RadioGroupContext = React.createContext(null);
const RadioGroup = React.forwardRef(({ onChange, children, value, readOnly, defaultValue, name, size = 'md', required, inline, className, ...rest }, ref) => {
    const nameId = React.useId();
    return (jsxRuntime.jsx(Fieldset.Fieldset, { readOnly: readOnly, size: size, className: className, ref: ref, ...rest, children: jsxRuntime.jsx(RadioGroupContext.Provider, { value: {
                value,
                defaultValue,
                name: name ?? `radiogroup-name-${nameId}`,
                onChange,
                required,
            }, children: jsxRuntime.jsx("div", { className: lite.clsx('fds-radio-group', inline && 'fds-radio-group--horizontal'), children: children }) }) }));
});
RadioGroup.displayName = 'RadioGroup';

exports.RadioGroup = RadioGroup;
exports.RadioGroupContext = RadioGroupContext;
