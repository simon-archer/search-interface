'use client';
'use strict';

var jsxRuntime = require('react/jsx-runtime');
var React = require('react');
var lite = require('../../node_modules/clsx/dist/lite.js');
var getSize = require('../../utilities/getSize.js');
var RovingTabindexRoot = require('../../utilities/RovingTabIndex/RovingTabindexRoot.js');

const ToggleGroupContext = React.createContext({});
/** `ToggleGroup` component.
 * @example
 * ```tsx
 * <ToggleGroup onChange={(value) => console.log(value)}>
 *   <ToggleGroup.Item value='1'>Toggle 1</ToggleGroup.Item>
 *   <ToggleGroup.Item value='2'>Toggle 2</ToggleGroup.Item>
 *   <ToggleGroup.Item value='3'>Toggle 3</ToggleGroup.Item>
 * </ToggleGroup>
 * ```
 */
const ToggleGroup = React.forwardRef(({ children, value, defaultValue, onChange, name, className, ...rest }, ref) => {
    const size = getSize.getSize(rest.size || 'md');
    const nameId = React.useId();
    const isControlled = value !== undefined;
    const [uncontrolledValue, setUncontrolledValue] = React.useState(defaultValue);
    let onValueChange = onChange;
    if (!isControlled) {
        onValueChange = (newValue) => {
            setUncontrolledValue(newValue);
            onChange?.(newValue);
        };
        value = uncontrolledValue;
    }
    return (jsxRuntime.jsx("div", { className: lite.clsx('fds-togglegroup', className), ref: ref, ...rest, children: jsxRuntime.jsxs(ToggleGroupContext.Provider, { value: {
                value,
                defaultValue,
                name: name ?? `togglegroup-name-${nameId}`,
                onChange: onValueChange,
                size,
            }, children: [name && (jsxRuntime.jsx("input", { className: 'fds-togglegroup__input', name: name, value: value })), jsxRuntime.jsx(RovingTabindexRoot.RovingTabindexRoot, { asChild: true, valueId: value, children: jsxRuntime.jsx("div", { className: 'fds-togglegroup__content', role: 'radiogroup', children: children }) })] }) }));
});
ToggleGroup.displayName = 'ToggleGroup';

exports.ToggleGroup = ToggleGroup;
exports.ToggleGroupContext = ToggleGroupContext;
