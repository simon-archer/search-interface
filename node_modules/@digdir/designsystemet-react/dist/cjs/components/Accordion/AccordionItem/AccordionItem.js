'use client';
'use strict';

var jsxRuntime = require('react/jsx-runtime');
var lite = require('../../../node_modules/clsx/dist/lite.js');
var React = require('react');

const AccordionItemContext = React.createContext(null);
const AccordionItem = React.forwardRef(({ children, className, open, defaultOpen = false, ...rest }, ref) => {
    const [internalOpen, setInternalOpen] = React.useState(defaultOpen);
    const contentId = React.useId();
    return (jsxRuntime.jsx("div", { className: lite.clsx('fds-accordion__item', (open ?? internalOpen) && 'fds-accordion__item--open', className), ref: ref, ...rest, children: jsxRuntime.jsx(AccordionItemContext.Provider, { value: {
                open: open ?? internalOpen,
                toggleOpen: () => {
                    if (open === undefined) {
                        setInternalOpen((iOpen) => !iOpen);
                    }
                },
                contentId: contentId,
            }, children: children }) }));
});
AccordionItem.displayName = 'AccordionItem';

exports.AccordionItem = AccordionItem;
exports.AccordionItemContext = AccordionItemContext;
