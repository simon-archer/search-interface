'use client';
'use strict';

var jsxRuntime = require('react/jsx-runtime');
var index = require('../../node_modules/@radix-ui/react-slot/dist/index.js');
var React = require('react');
var lite = require('../../node_modules/clsx/dist/lite.js');
var PaginationRoot = require('./PaginationRoot.js');

const PaginationContent = React.forwardRef(({ asChild, className, ...rest }, ref) => {
    const Component = asChild ? index.Slot : 'ul';
    const { size } = React.useContext(PaginationRoot.PaginationContext);
    return (jsxRuntime.jsx(Component, { ref: ref, className: lite.clsx('fds-pagination', `fds-pagination--${size}`, className), ...rest }));
});

exports.PaginationContent = PaginationContent;
